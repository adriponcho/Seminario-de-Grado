<%= form_for(@criterio) do |f| %>
  <% if @criterio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@criterio.errors.count, "error") %> prohibited this criterio from being saved:</h2>

      <ul>
      <% @criterio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion, :size => "10x10" %>
  </div>
  <div class="field">
    <% tipos = {'evaluacion' => 'evaluacion', 'impacto' => 'impacto', 'aceptacion' => 'aceptacion'} %>
    <%= f.label :tipo %><br />
    <%= f.select :tipo , tipos %>
  </div>
  <!--<div class="field">
    <%= f.label :estado %><br />
    <%= f.check_box :estado %>
  </div>-->
   <div class="actions">
    <button class = "btn btn-inverse" type = "f.submit">
       Crear criterio
    </button>
  </div>
<% end %>
