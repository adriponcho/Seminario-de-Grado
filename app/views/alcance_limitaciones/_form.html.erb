<%= form_for(@alcance_limitacione) do |f| %>
  <% if @alcance_limitacione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alcance_limitacione.errors.count, "error") %> prohibited this alcance_limitacione from being saved:</h2>

      <ul>
      <% @alcance_limitacione.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="hero-unit">
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <% tipos = {'Alcance' => 'alcance', 'Limitacion' => 'limitacion'} %>
    <%= f.label :tipo %><br />
    <%= f.select :tipo , tipos %>
  </div>
  <!--<div class="field">
    <%= f.label :estado %><br />
    <%= f.check_box :estado %>
  </div>-->
  <div class="actions">
     <button class = "btn btn-inverse" type = "f.submit">
        Crear
     </button>
  </div>
</div>
<% end %>
